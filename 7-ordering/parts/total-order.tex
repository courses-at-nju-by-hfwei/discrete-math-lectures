% total-order.tex

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[全序关系 (Total Order)]
    令 $\red{\preceq} \;\subseteq X \times X$ 是 $X$ 上的\blue{偏序}。\\[5pt]
    如果 $\preceq$ 满足以下\red{连接性}, 则称 $\preceq$ 是 $X$ 上的\red{全序关系}:

    \[
      \forall a, b \in X.\; a \preceq b \lor b \preceq a.
    \]
  \end{definition}

  \[
    (\R, \le) \qquad (\R, \ge)
  \]
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[严格全序关系 (Strict Total Order)]
    令 $\red{\prec} \;\subseteq X \times X$ 是 $X$ 上的\blue{关系}。\\[3pt]
    如果 $\prec$ 满足以下条件, 则称 $\prec$ 是 $X$ 上的\red{严格全序关系}: \\[6pt]
    \begin{description}
      \item[(1) 反自反 (Irreflexivity):]
        \[
          \forall x \in X.\; \lnot (x \prec x)
        \]
      \item[(2) 传递性 (Transitive):]
        \[
          \forall x, y, z \in X.\; x \prec y \land y \prec z \to x \prec z
        \]
      \item[(3) 三歧性 (Trichotomous):]
        \[
          \forall x, y \in X.\;
            (\text{\red{exactly one of}}\; x \prec y, x = y, \;\text{or}\; y \prec x \text{ holds})
        \]
    \end{description}
  \end{definition}

  \[
    (\R, <) \qquad (\R, >)
  \]
\end{frame}
%%%%%%%%%%%%%%%