% well-ordering.tex

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{definition}[良序 (Well-Ordering)]
    设 $(X, \preceq)$ 是\blue{全序集}。 \\[5pt]
    如果$X$ 的任意\cyan{非空}子集都有最小元, 则称 $(X, \preceq)$ 为\red{良序集}。
  \end{definition}

  \pause
  \[
    (\N, \le) \qquad \gray{\Z, \le} \qquad \gray{\R, \le}
  \]

  \pause
  \vspace{0.50cm}
  \begin{center}
    数学归纳法 \qquad 良序集
  \end{center}
\end{frame}
%%%%%%%%%%%%%%%